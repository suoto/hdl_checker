[tox]
envlist = {py27,py37}-{msim,ghdl,xvhdl,fallback,standalone}-{linux,windows}

[testenv]
platform =
    linux: linux
    windows: win32|win64

passenv =
  HOME
  CI
  APPVEYOR
  TRAVIS
  GIT_URL

setenv = 
    GIT_URL = {env:GIT_URL:https://github.com/suoto}
    
# install pytest in the virtualenv where commands will be executed
deps =
    coverage==4.1
    mock==2.0.0
    nose2-cov==1.0a4
    nose2==0.6.5
    six==1.10.0
    testfixtures==4.10.0
    vunit-hdl==0.66.0
    unittest2==1.1.0
    WebTest==2.0.23
    git+https://github.com/suoto/rainbow_logging_handler

# NOTE: you can run any command line tool here - not just tests
whitelist_externals =
  linux: bash
  linux-!local: cp

  windows-!local: Xcopy

  !local: git

  local: tox

commands_pre:

  linux-{msim,ghdl,xvhdl}: git clone --depth 1 --quiet {env:GIT_URL}/grlib "{envtmpdir}/grlib"
  linux: git clone --depth 1 --quiet {env:GIT_URL}/vim-hdl-examples "{envtmpdir}/vim-hdl-examples"
  # To remove git sutff so git clean -fdx can clean that
  linux: bash -c 'rm -rf $(find {envtmpdir} -iname "\.git")'

  windows-{msim,ghdl,xvhdl}: git clone --depth 1 {env:GIT_URL}/grlib "{envtmpdir}\\grlib"
  windows: git clone --depth 1 {env:GIT_URL}/vim-hdl-examples "{envtmpdir}\\vim-hdl-examples"

commands =
  local: tox -p5 -e py27-xvhdl-linux -e py37-xvhdl-linux -e py27-ghdl-linux -e py27-msim-linux -e py27-fallback-linux -e py27-standalone-linux -e py37-fallback-linux -e py37-standalone-linux -e py37-ghdl-linux -e py37-msim-linux

  msim:       python .ci/scripts/run_tests.py -v --log-level DEBUG --msim
  ghdl:       python .ci/scripts/run_tests.py -v --log-level DEBUG --ghdl
  fallback:   python .ci/scripts/run_tests.py -v --log-level DEBUG --fallback
  standalone: python .ci/scripts/run_tests.py -v --log-level DEBUG --standalone
  xvhdl:      python .ci/scripts/run_tests.py -v --log-level DEBUG --xvhdl
